{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Article {% endblock %}

{% block body %}
    <div class="center">
        <div id="article-index">
            <img src="../{{article.image}}" alt="" />
            <div id="article-description">
                <h1> {{ article.title }} </h1>
                <br/>
                <p> {{ article.description }} </p>
                <br/>
                <p> <span class="cost"> {{ article.price }} € </span> <a @click="count++" href= "{{ path('cart_add', {'id': article.id}) }}"><i class="fa-solid fa-cart-plus fa-xl"></i></a></p>
            </div>

        </div>
    </div>
{% endblock %}